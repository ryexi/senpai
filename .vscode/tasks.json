{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "kio --b debug",
            "problemMatcher": "$msCompile"
        },
        {
            "label": "publish",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "kio --b publish",
            "problemMatcher": "$msCompile"
        }
    ]
}